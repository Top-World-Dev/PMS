import doctorSchema from "./schema.doctor";
import patientSchema from "./schema.patient";
import tokenSchema from "./schema.token";
import chamberSchema from "./schema.chamber";

export const DoctorSchema = doctorSchema;

export const PatientSchema = patientSchema;

export const TokenSchema = tokenSchema;

export const ChamberSchema = chamberSchema;